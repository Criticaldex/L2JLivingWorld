<?xml version="1.0" encoding="UTF-8"?>
<!--
	AUTO-GENERATED by tools/build_populations.py from dist/game/data/spawns.
	Field-hunter <population> groups: center, level band and count are derived
	from where the server actually spawns monsters. Territory (polygon) fields
	are emitted as real <point> polygons; fixed-coordinate camps as circles.

	LOADING: set PhantomAutoHuntingZones = True in config/Custom/FakePlayers.ini
	and the server loads this file ADDITIVELY, after your hand-authored
	PhantomPopulations.xml, so buddies/regulars/friends there are kept and only
	field hunters are added. (Or test the old way: back up PhantomPopulations.xml
	and copy this over it, then //phantom reload.)

	Safe to hand-edit or to merge chosen lines into your own file. Tune it with the
	density, max-per-zone, min-radius and max-radius options (run the tool with -h).
-->
<list>
	<!-- Hunting-zone populations disabled; city populations remain in FakePlayerBehavior.xml. -->
</list>
